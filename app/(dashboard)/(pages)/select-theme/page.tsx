import ThemePreview from "@/components/ThemePreview";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <ThemePreview />
    </div>
  );
};

export default Page;
